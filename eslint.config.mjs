import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // "Fetch on mount, setState in the loader" is the standard client-side
      // data-fetching pattern used throughout this app's pages — downgrade
      // rather than restructure every effect around it.
      "react-hooks/set-state-in-effect": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    // backend/ is a separate package with its own lint setup.
    "backend/**",
  ]),
]);

export default eslintConfig;
